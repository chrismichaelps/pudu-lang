---
type: handoff
tags: [handoff, tooling, documentation]
---

# Documentation Site Handoff

## Objective

Complete the browser surface promised by the existing documentation index: one static,
self-contained, Hoogle-like page produced by `pudu doc --html`.

## FMCF Roles

- **Language Architect / Tooling Architect:** completed the [[Doc Site]] contract, CLI surface,
  offline boundary, search parity, and injection-resistance Grill Log; handed off to Tooling.
- **Tooling Engineer:** completed the issue #93 implementation; owned `src/Pudu/Doc/Site.hs`, the documented
  result-only correction in `src/Pudu/Doc/Query.hs`, `app/Main.hs`, `pudu.cabal`, and focused tests.
- **Forensic Guardian:** completed the post-implementation audit of code/wiki fidelity, backlinks,
  MOCs, changelog, and validation evidence with no unresolved P0/P1/P2 findings.

## Resolved Contract

- Emit one HTML document to stdout through `pudu doc --html <file>...`.
- Embed the existing structured JSON index safely; do not create a second schema.
- Preserve name and type-shape search semantics in the browser and reflect queries in `?q=`.
- Require no server, framework, network request, external asset, or generated image.

## Ownership

The implementation lane owns only the new renderer, the existing query contract's result-only
correction, CLI wiring, package exposure, documentation tests, and their vault projections. It must
not alter language syntax, typing, evaluation, or the standard-library modules in the two lower
stacked branches.

## Exact Next Action

Commit and push the reconciled issue #93 branch, open its intentionally stacked PR against #92's
branch, and require the locked GHC 9.14.1 CI gate before merge.

## Validation State

- Full property suite passes locally at `-O0` and `-O2` with the available GHC 9.10.3.
- The executable Node 22 harness passes name, concrete shape, result-only score, Unicode, malformed,
  hostile-depth, deep-link, missing-signature, and rendered-index checks.
- Missing-root and zero-input HTML commands return non-zero while recoverable output remains intact.
- The local GHC 9.14.1 install exhausted available disk while unpacking; the repository CI owns the
  exact locked compiler/Cabal gate before merge.

## Referenced by

[[handoffs/_MOC]] · [[Engineering Delivery]] · [[Doc Site]]
