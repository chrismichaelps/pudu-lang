module RunsStd

import Std.List {sumOr, sorted}
import Std.Math as Math

export fn main() -> Int {
  let items = [3, 1, 4, 1, 5]
  let ordered = sorted(&items)
  Math.gcd(sumOr(&items, 0), 12) + ordered[0] + Math.pow(2, 5)
}
