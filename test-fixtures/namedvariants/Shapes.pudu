module Shapes

/// A sum whose variants name their payloads, exported so another module builds
/// and matches them across the boundary.
export type Shape
  = Circle{radius: Int}
  | Rect{width: Int, height: Int}
  | Point
